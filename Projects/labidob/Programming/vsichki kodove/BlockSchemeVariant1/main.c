#include <stdio.h>
#include <stdlib.h>

int main()
{
    for(int k=1;k<n;k++){
    {
        if(k==1) r = int m[k] / m[k-1];
        else if (m[k] / m[k-1] != r)
        {
            fl = 0;
            break;
        }
    }
}
