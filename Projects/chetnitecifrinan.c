#include <stdio.h>
int main ()
{
    int n,m=0;
    printf("vuvedi n: ");
    scanf("%d",&n);
    while(n>0)
    {
        if("(n%10)%2==0");
        {
            printf("\nchisloto %d e chetno",n);
        }
        n=n/10;
    }

 return 0;
}
